package config

// Config is the placeholder engine config for the current project skeleton.
// It will be expanded as the standalone engine and server are introduced.
type Config struct {
	DBPath string
}
